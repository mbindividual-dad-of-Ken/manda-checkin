# 彈性習慣 PWA — GitHub Pages 部署指南

## 前置準備

1. 確認已安裝 Git：在 PowerShell 執行 `git --version`
   - 若尚未安裝：前往 https://git-scm.com 下載安裝
2. 確認有 GitHub 帳號：https://github.com
   - 若尚未申請，請先免費註冊

---

## Step 1：在 GitHub 建立新 Repository

1. 登入 GitHub → 右上角 **+** → **New repository**
2. Repository name 填：`flexi-habit`（或任意名稱）
3. 設定為 **Public**（GitHub Pages 免費版需要 Public）
4. **不要**勾選 "Add README file"（我們已有檔案）
5. 點 **Create repository**
6. 複製畫面上顯示的 repository URL，例如：
   `https://github.com/你的帳號/flexi-habit.git`

---

## Step 2：在本機初始化 Git 並推送

開啟 **PowerShell** 或 **命令提示字元**，執行以下指令：

```powershell
# 1. 進入專案資料夾
cd "C:\Users\USER\Desktop\@AI協作\@Project-人生漫談\彈性習慣-PWA"

# 2. 如果已有 .git 資料夾，先刪除（本次需要重新初始化）
Remove-Item -Recurse -Force .git -ErrorAction SilentlyContinue

# 3. 初始化 Git
git init
git branch -M main

# 4. 設定你的 Git 身份（只需做一次）
git config --global user.name "Jerry"
git config --global user.email "mbindividual@gmail.com"

# 5. 新增所有檔案
git add .

# 6. 第一次 commit
git commit -m "🚀 初始部署：彈性習慣 PWA"

# 7. 加入 GitHub 遠端（替換成你的 repo URL）
git remote add origin https://github.com/你的帳號/flexi-habit.git

# 8. 推送
git push -u origin main
```

---

## Step 3：開啟 GitHub Pages

1. 到你的 GitHub repository 頁面
2. 點上方 **Settings** → 左側選單 **Pages**
3. Source 選 **Deploy from a branch**
4. Branch 選 **main** / `/ (root)`
5. 點 **Save**
6. 等 1-2 分鐘後，頁面會顯示：
   > **Your site is live at** `https://你的帳號.github.io/flexi-habit/`

---

## Step 4：更新 GA4 Tracking ID（重要）

網站上線後，把 `index.html` 中的佔位符替換成真實 ID：

1. 前往 https://analytics.google.com → 建立 GA4 資源
2. 取得 Measurement ID（格式：`G-XXXXXXXXXX`）
3. 在 index.html 搜尋 `G-XXXXXXXXXX`，替換成你的真實 ID
4. 再次 commit 並推送：

```powershell
git add index.html
git commit -m "✨ 加入 GA4 追蹤 ID"
git push
```

---

## Step 5：測試 PWA 安裝

部署完成後，用手機瀏覽器開啟網址：

**Android（Chrome）：**
- 瀏覽器會自動顯示「加入主畫面」提示
- 或點右上角選單 → 「安裝應用程式」

**iOS（Safari）：**
- 開啟 Safari → 點底部分享按鈕 → 「加入主畫面」

**桌機（Chrome/Edge）：**
- 網址列右側會出現安裝圖示
- 或點瀏覽器選單 → 「安裝彈性習慣」

---

## 後續更新流程

每次修改 `index.html` 後，只需執行：

```powershell
cd "C:\Users\USER\Desktop\@AI協作\@Project-人生漫談\彈性習慣-PWA"
git add .
git commit -m "說明這次改了什麼"
git push
```

GitHub Pages 會在 1-2 分鐘內自動更新。

---

## 常見問題

**Q：推送時要求輸入密碼？**
A：GitHub 已停止密碼驗證，需要使用 Personal Access Token：
Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token
權限勾選 `repo`，產生後把 token 當密碼使用。

**Q：Service Worker 沒有更新？**
A：修改 `sw.js` 中的 `CACHE_NAME` 版本號（如 `flexi-habit-v2`），強制用戶端更新快取。

**Q：想用自訂網域？**
A：GitHub Pages 支援自訂域名（Settings → Pages → Custom domain），
需要在域名管理商設定 CNAME 指向 `你的帳號.github.io`。
